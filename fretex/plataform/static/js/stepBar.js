const barItems = document.querySelectorAll(".stepBar-item");
const etapasForm = document.querySelectorAll("#step");

if (barItems.length && etapasForm.length) {
    etapasForm.forEach((etapa, index) => {

    })
}